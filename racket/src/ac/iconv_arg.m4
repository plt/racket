AC_ARG_ENABLE(iconv,      [  --enable-iconv          use iconv (usually auto-enabled)])
AC_ARG_ENABLE(icu,        [  --enable-icu            build iconv-via-ICU support (auto-enabled on Windows)])
AC_ARG_ENABLE(icu-dll,    [  --enable-icu-dll        dynamically load ICU (Windows-only, automatic)])
